spring:
  datasource:
    url: jdbc:mysql://localhost:3306/e_commerce_system
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  security:
    oauth2:
      client:
        registration:
          google:
            scope:
              - profile
              - email
            #this line must match with the URIs on GG API (Authorized redirect URIs)
            redirect-uri: http://localhost:8080/login/oauth2/code/google
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#    database: mysql
#    database-platform: org.hibernate.dialect.MySQLDialect






